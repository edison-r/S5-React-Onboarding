export interface Feature {
    title: string;
    description: string;
    src: string;
    alt: string;
}

export interface Ads{
    browser: string,
    title: string;
    version: string;
    src: string;
    alt: string;
}

export interface FAQItem {
    title: string;
    description: string;
}
